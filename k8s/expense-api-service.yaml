apiVersion: v1
kind: Service
metadata:
  name: expense-api
  labels:
    app: expense-api
spec:
  type: NodePort
  selector:
    app: expense-api
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
